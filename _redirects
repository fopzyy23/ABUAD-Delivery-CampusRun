# ============================================================
# INTENTIONALLY COMMENT-ONLY — DO NOT ADD RULES HERE.
# ============================================================
# Netlify processes rules from this file BEFORE the rules in netlify.toml,
# so having the same rules in both places means one set silently shadows the
# other and only one file can ever be edited safely.
#
# The effective routing rules now live in exactly ONE place:
#
#   netlify.toml  ->  [[redirects]]
#
# This file is kept (rather than deleted) as a signpost, so nobody re-adds a
# second, conflicting rule set here. Comments are ignored by Netlify, so this
# file contributes zero rules.
# ============================================================
